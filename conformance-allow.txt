# conformance-allow.txt -- the static ratchet for protoClojure.
#
# Format: <check-id> <path>:<line> <sha1-12-of-the-line-text> :: <justification>
#
# The hash is of the LINE TEXT, so an entry goes stale when the line changes and
# not only when it moves.  The raw count is never the gate; the delta is.
#
# Run: python3 ../protoCore/scripts/conformance/check_static.py --repo .
# Exit 0 clean, 1 an unjustified finding, 2 a stale entry.

blocking_join_unbracketed src/runtime/GCCensus.cpp:59 2c00e6f71f93 :: Correct, and cheaply so, but the reason is not obvious from the line. The joined thread is the PROTOCLJ_GC_STATS sampler, a raw std::thread that touches no ProtoObject* -- it reads space_.getGCCycleCount(), reclaimedLastCycle, heapSize and liveCellsLastCycle, nothing else -- so it is not registered with protoCore and is not in runningThreads. The joining thread (main, which IS registered) therefore holds the quorum for at most the 200 us the sampler needs to observe stop_ and exit, which is bounded and bounded small. It would stop being correct if the sampler ever held a ProtoObject* or grew an unbounded wait, at which point the join needs a ProtoContext::UnmanagedScope. Reviewed 2026-09-25, agent, pending maintainer review.
blocking_join_unbracketed src/runtime/StackGuard.cpp:146 07a115653b2c :: Correct, but only by construction. This pthread_join is main()'s thread waiting for the evaluator thread that runOnEvaluatorThread spawned to get a larger stack. The ProtoSpace is constructed INSIDE the joined thread (src/main.cpp), so at the moment of the join the joining thread is registered with no space and there is no quorum for it to hold. It must stay that way: if protoClojure ever constructs the space before this join, this call needs a ProtoContext::UnmanagedScope and this entry must be deleted rather than re-hashed. Reviewed 2026-09-25, agent, pending maintainer review.

attr_sentinel src/runtime/Primitives.cpp:* * :: File-level entry covering four attr_sentinel WARNINGS (not errors) in this file: a getAttribute result tested as a C++ boolean. Each was read: all four are `if (!v || v == PROTO_NONE)`-equivalent guards where the PROTO_NONE test lives more than four lines away or inside a helper, so the check cannot see it and reports a heuristic hit. The check documents its own unbounded false-positive rate for this shape and is `warn` for exactly that reason. A file-level entry is used deliberately: four individual justifications would be a ritual. The ratchet still closes on a NEW FILE. Reviewed 2026-09-25, agent, pending maintainer review.
